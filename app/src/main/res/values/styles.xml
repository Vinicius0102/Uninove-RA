<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="the">
        <item name="colorControlNormal">#1C1C1C</item>
        // É a cor padrão da linha que fica embaixo do EditText

        <item name="colorControlActivated">#CD5555</item>
        // Cor da linha e do texto hint quando receber foco(clique)

        <item name="android:textColorHint">#8B0000</item>
        // É a cor normal do texto hint
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
